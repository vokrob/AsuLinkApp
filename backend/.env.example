# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True

# Email Configuration
# Для разработки можно использовать console backend
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Для реальной отправки email через Gmail:
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=vokrob.dev@gmail.com
EMAIL_HOST_PASSWORD=zwti trnj gkqu pelk
DEFAULT_FROM_EMAIL=AsuLinkApp <vokrob.dev@gmail.com>

# Для других SMTP провайдеров:
# EMAIL_HOST=smtp.your-provider.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@your-provider.com
# EMAIL_HOST_PASSWORD=your-password
# DEFAULT_FROM_EMAIL=AsuLinkApp <your-email@your-provider.com>

# Database (по умолчанию используется SQLite)
# DATABASE_URL=sqlite:///db.sqlite3

# Для PostgreSQL:
# DATABASE_URL=postgres://user:password@localhost:5432/asulinkapp

# Для MySQL:
# DATABASE_URL=mysql://user:password@localhost:3306/asulinkapp
